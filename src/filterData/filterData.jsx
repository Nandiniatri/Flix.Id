import { useContext } from "react";
import { headerCenterContext } from "../contextApi/UseHeadercenter";

const FilterData = () => {
    const { movie } = useContext(headerCenterContext);
    
    return (
        <div>

        </div>
    )
}

export default FilterData;